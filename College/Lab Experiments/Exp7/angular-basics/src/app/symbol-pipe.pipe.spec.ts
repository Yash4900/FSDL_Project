import { SymbolPipePipe } from './symbol-pipe.pipe';

describe('SymbolPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SymbolPipePipe();
    expect(pipe).toBeTruthy();
  });
});
